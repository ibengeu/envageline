services:
  kokoro:
    build: .
    container_name: pdf-reader-kokoro
    ports:
      - "8880:8880"
    volumes:
      - ./models:/models
environment:
      KOKORO_DEFAULT_VOICE: af_heart
      KOKORO_MODEL_DIR: /models
